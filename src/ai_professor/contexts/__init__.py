"""The three deliberately isolated contexts (DESIGN.md §3):

- ``extractor`` — curriculum-generation; builds the DAG from sources. The single trust root.
- ``professor`` — teaches one topic; subordinate; may propose advancement but never grant it.
- ``judger`` — decides readiness in a lean, isolated, adversarial context; sole ledger writer.
"""
